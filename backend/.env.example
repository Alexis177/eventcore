# --- Servidor ---
PORT=3000
NODE_ENV=development

# --- Base de Datos (Supabase / PostgreSQL) ---
DB_HOST=db.xxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# --- JWT ---
JWT_SECRET=cambia_esto_por_una_clave_muy_segura_y_larga
JWT_EXPIRES_IN=24h
